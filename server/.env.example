# Gmail: create an App Password at https://myaccount.google.com/apppasswords
# Use that as SMTP_PASS (and your Gmail as SMTP_USER). No quotes around the password.
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Optional: "From" address (defaults to SMTP_USER)
# SMTP_FROM=awadhetawy@gmail.com

# Schedule: which days and hours are bookable
# OPEN_DAYS: 0=Sun, 1=Mon, 2=Tue, 3=Wed, 4=Thu, 5=Fri, 6=Sat (comma-separated). Default: Mon–Sat
OPEN_DAYS=1,2,3,4,5,6
# START_TIME / END_TIME: 24h format "HH:mm". Default: 12:00–21:00
START_TIME=12:00
END_TIME=21:00
# SLOT_MINUTES: interval between time slots (5–120). Default: 30
SLOT_MINUTES=45

# Optional: require a key when saving hours from the "Configure hours" page
# ADMIN_SECRET=your-secret-key

PORT=3001

ADMIN_SECRET=your-secret-key-here
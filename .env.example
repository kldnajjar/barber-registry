# Vercel Environment Variables Configuration
# Copy this file and configure the values in your Vercel project settings
# Dashboard: https://vercel.com/[your-project]/settings/environment-variables

# ============================================================================
# REQUIRED: Database Configuration
# ============================================================================

# Vercel Postgres connection string (automatically provided by Vercel Postgres)
# Format: postgres://user:password@host:port/database
# Get this from: Vercel Dashboard > Storage > Postgres > .env.local tab
POSTGRES_URL=postgres://default:password@host.postgres.vercel-storage.com:5432/verceldb

# ============================================================================
# REQUIRED: Frontend Configuration
# ============================================================================

# Google OAuth Client ID for authentication
# Get from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application)
# Add your Vercel domain to authorized origins (e.g., https://your-app.vercel.app)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================================================
# OPTIONAL: Email Notification Configuration
# ============================================================================

# SMTP server configuration for sending booking notification emails
# If not configured, bookings will still be saved but no emails will be sent

# Gmail example: use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password

# Optional: "From" address (defaults to SMTP_USER if not set)
# SMTP_FROM=your-email@gmail.com

# ============================================================================
# OPTIONAL: Admin Panel Protection
# ============================================================================

# Secret key for protecting admin operations (schedule configuration updates)
# If not set, admin endpoints will be unprotected (development mode)
# Generate a secure random string for production
ADMIN_SECRET=your-secure-random-secret-key

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================

# 1. Set these variables in Vercel Dashboard under Settings > Environment Variables
# 2. POSTGRES_URL is automatically provided when you add Vercel Postgres storage
# 3. VITE_ prefixed variables are exposed to the frontend build
# 4. Run the migration script before first deployment: npm run migrate
# 5. Free tier limits:
#    - Vercel: 100 GB bandwidth/month, 100 hours function execution/month
#    - Postgres: 256 MB storage, 60 hours compute/month
